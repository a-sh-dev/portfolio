import tw, { styled } from 'twin.macro';

const Wrapper = styled.div(() => [
  tw`
  
  `,
]);

const JournalCard = () => {
  return (
    <div>
      <h1>JOURNAL CARD</h1>
    </div>
  );
};

export default JournalCard;
