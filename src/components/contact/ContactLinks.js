import tw, { styled } from 'twin.macro';

const Wrapper = styled.div(() => [
  tw`
  
  `,
]);

const ContactLinks = () => {
  return (
    <div>
      <h1>
        Let&apos;s be social! Please connect with me through the quick links
        below! (there are massive, so you won&apos;t miss it!)
      </h1>
    </div>
  );
};

export default ContactLinks;
