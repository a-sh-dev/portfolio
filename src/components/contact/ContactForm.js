import tw, { styled } from 'twin.macro';

const Wrapper = styled.div(() => [
  tw`
  
  `,
]);

const ContactForm = () => {
  return (
    <div>
      <h1>CONTACT FORM</h1>
    </div>
  );
};

export default ContactForm;
